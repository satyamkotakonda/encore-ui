# Fail updating paperless settings
-   request:
        url: ^/api/billing/020-8675309/billInfo$
        method: PUT
    response:
        status: 404

# Get Billing Information for an Account
-   request:
        url: ^/api/billing/(.*)/billInfo$
        method: [GET, PUT]
    response:
        status: 200
        headers:
            content-type: application/json
        file: test/api-mocks/responses/billing/bill-info.json

# Fail updating notifications
-   request:
        url: ^/api/billing/020-8675309/paymentInfo$
        method: PUT
    response:
        status: 404

# Get Payment Information for an Account
-   request:
        url: ^/api/billing/(.*)/paymentInfo$
        method: [GET, PUT]
    response:
        status: 200
        headers:
            content-type: application/json
        file: test/api-mocks/responses/billing/payment-info.json
